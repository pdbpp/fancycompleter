image: themattrix/tox:latest

pipelines:
  default:
    - step:
        script:
          - pip --version
          - pip install -U setuptools
          - pip install -U mercurial
          - tox --version
          - tox
